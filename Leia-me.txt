Necessário possuir Python (https://www.python.org/).
Bem como os pacotes:

- Django
- Django REST framework
- Pillow

Que podem ser baixados da seguinte maneira:

$ pip install django
$ pip install djangorestframework
$ pip install pillow
$ pip install drf-base64

Para executar (na pasta BackEnd):

$ python manage.py runserver

Basta acessar a URL: http://127.0.0.1:8000/home/